/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.vikrin.com', // always use www if that's your canonical domain
  generateRobotsTxt: true,
  changefreq: 'weekly',
  priority: 0.7,
};